en:
  no_cms:
    admin:
      blocks:
        blocks:
          index:
            new_block: New block
            title: Blocks
          nested_index:
            new_block: New block
            title: Blocks
    blocks:
      blocks:
        layouts:
          default: 'Default block'
      template_zones:
        body: Body
        footer: Footer
        header: Header
      templates:
        default: Default
